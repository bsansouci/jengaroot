(library
 ((name sexplib)
  (public_release ((status (build_and_install_as sexplib))
                   (extra_deps (bigarray))))
  (ocamllex (lexer))
  (ocamlyacc (parser parser_with_layout))
  (preprocess (((command ${PA_CPP}) (List (pre_sexp)))))))
