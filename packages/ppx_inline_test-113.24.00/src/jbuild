(library
 ((name ppx_inline_test)
  (public_release ((status (build_and_install_as ppx_inline_test))
                   (kind ppx_rewriter)))
  (wrapped false)
  (libraries (ppx_core
              ppx_driver
              ppx_inline_test_libname))
  (ocaml_includes (+compiler-libs))
  (preprocess ((metaquot All)))))
