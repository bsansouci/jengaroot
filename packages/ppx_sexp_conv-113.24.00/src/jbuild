(library
 ((name ppx_sexp_conv)
  (public_release ((status (build_and_install_as ppx_sexp_conv))
                   (kind ppx_type_conv_plugin)))
  (wrapped false)
  (libraries (ppx_core ppx_type_conv ppx_sexp_conv_expander))
  (ocaml_includes (+compiler-libs))
  (preprocess ((no_preprocessing All)))))
