(library
 ((name ppx_sexp_conv_expander)
  (public_release ((status (build_and_install_as ppx_sexp_conv.expander))
                   (kind ppx_library)))
  (wrapped false)
  (libraries (ppx_core))
  (ocaml_includes (+compiler-libs))
  (preprocess ((metaquot All)))))
