(library
 ((name ppx_here_expander)
  (public_release ((status (build_and_install_as ppx_here.expander))
                   (kind ppx_library)))
  (wrapped false)
  (libraries (ppx_driver ppx_core))
  (ocaml_includes (+compiler-libs))
  (preprocess ((no_preprocessing All)))))
