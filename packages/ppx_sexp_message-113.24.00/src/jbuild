(library
 ((name ppx_sexp_message)
  (public_release ((status (build_and_install_as ppx_sexp_message))
                   (kind ppx_rewriter)))
  (wrapped false)
  (libraries (ppx_core ppx_driver ppx_sexp_conv_expander ppx_here_expander))
  (ocaml_includes (+compiler-libs))
  (preprocess ((metaquot All)))))
