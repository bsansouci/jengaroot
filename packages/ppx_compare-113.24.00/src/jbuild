(library
 ((name ppx_compare)
  (public_release ((status (build_and_install_as ppx_compare))
                   (kind ppx_type_conv_plugin)))
  (wrapped false)
  (libraries (ppx_core
              ppx_driver
              ppx_type_conv
              ppx_compare_expander))
  (ocaml_includes (+compiler-libs))
  (preprocess ((no_preprocessing All)))))
