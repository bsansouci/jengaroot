;; This module is linked by jenga in the test runners of libraries that use expect
;; tests. jenga assumes there is a single module in here called evaluator.
(executables (
  (names (evaluator))
  (libraries (expect_test_evaluator_lib))
))
