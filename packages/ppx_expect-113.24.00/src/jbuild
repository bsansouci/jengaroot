
(library (
  (name ppx_expect)
  (public_release ((status (build_and_install_as ppx_expect))
                   (kind ppx_rewriter)))
  (libraries (ppx_core ppx_driver ppx_inline_test ppx_inline_test_libname))
  (ocaml_includes (+compiler-libs))
  (preprocess ((metaquot All)))
))
