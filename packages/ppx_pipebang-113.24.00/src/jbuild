(library
 ((name ppx_pipebang)
  (public_release ((status (build_and_install_as ppx_pipebang))
                   (kind ppx_rewriter)))
  (wrapped false)
  (libraries (ppx_core ppx_driver))
  (ocaml_includes (+compiler-libs))
  (preprocess ((metaquot All)))))
