(library
 ((name ppx_driver_ocamlbuild)
  (wrapped false)
  (public_release ((status (build_and_install_as ppx_driver.ocamlbuild))
                   (extra_deps (ocamlbuild))))
  (libraries (ppx_driver))
  (flags (:standard -I +ocamlbuild))
  (preprocess ((no_preprocessing All)))))
