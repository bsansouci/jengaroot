(rule
 ((targets (ast_lifter.ml))
  (deps (genlifter.exe))
  (action "./genlifter.exe -I +compiler-libs Parsetree.expression > ast_lifter.ml")))

(executables
 ((names (genlifter ppx_metaquot))
  (extra_disabled_warnings (17))
  (extra_ocaml_includes (+compiler-libs))
  (link_flags (ocamlcommon.cmxa))
  (preprocess ((no_preprocessing All)))
  (libraries (ppx_tools))))
