(library
 ((name ppx_assert)
  (public_release ((status (build_and_install_as ppx_assert))
                   (kind ppx_rewriter)))
  (wrapped false)
  (libraries (ppx_core
              ppx_driver
              ppx_sexp_conv_expander
              ppx_here_expander
              ppx_compare_expander))
  (ocaml_includes (+compiler-libs))
  (preprocess ((metaquot All)))))
